
streams_directory: "./config/streams/pretrain_era5_georing_avhrr/"

general:

  # mutable parameters
  istep: 0
  rank: ???
  world_size: ???

training_config:
  num_mini_epochs: 4
  samples_per_mini_epoch: 8192

  context_loss:
    enabled: true
    weight: 0.4
    warmup_steps: 2000             # linear warmup from 0 to weight over this many steps
