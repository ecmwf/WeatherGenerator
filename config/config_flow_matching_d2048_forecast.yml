# (C) Copyright 2025 WeatherGenerator contributors.
#
# This software is licensed under the terms of the Apache Licence Version 2.0
# which can be obtained at http://www.apache.org/licenses/LICENSE-2.0.
#
# In applying this licence, ECMWF does not waive the privileges and immunities
# granted to it by virtue of its status as an intergovernmental organisation
# nor does it submit to any jurisdiction.
#
# Flow matching / score matching, d2048, forecast conditioning.
# Based on config_diffusion_d2048_forecast_concat_noskip.yml — identical architecture
# (d2048, fe_num_blocks=12, forecast + concatenate conditioning, iy534ge3 backbone); only the
# generative formulation differs: EDM diffusion -> Gaussian-path flow matching.
#
# Deltas vs the diffusion config it is based on:
#   * fe_flow_matching_model: True          -> routes the forecast engine to FlowMatchingForecastEngine
#                                              (fe_diffusion_model stays True: it builds the shared
#                                               time-conditioned DiT backbone in ForecastingEngine)
#   * fe_flow_* / fm_*                      -> Gaussian path, parameterization, sampler
#   * noise_distribution: uniform           -> masking.py draws t ~ U[0,1] at the source (it IS the path time)
#   * losses.latent_diff                    -> LossFlowMatching + FlowMatchingTargetEncoder
#   * validation_noise_levels               -> fixed path times t in (0,1), NOT eta values
#   * fe_diffusion_model_no_skip_connection -> DROPPED: flow matching has no EDM preconditioning at
#                                              all (it regresses the raw velocity/noise/score), so it
#                                              is inherently "no skip"; the flag would be a no-op.

embed_orientation: "channels"
embed_unembed_mode: "block"
embed_dropout_rate: 0.1

ae_local_dim_embed: 2048
ae_local_num_blocks: 0
ae_local_num_heads: 16
ae_local_dropout_rate: 0.1
ae_local_with_qk_lnorm: True

ae_local_num_queries: 1
ae_local_queries_per_cell: False
ae_adapter_num_heads: 16
ae_adapter_embed: 128
ae_adapter_with_qk_lnorm: True
ae_adapter_with_residual: True
ae_adapter_dropout_rate: 0.1

ae_global_dim_embed: 2048
ae_global_num_blocks: 4
ae_global_num_heads: 32
ae_global_dropout_rate: 0.1
ae_global_with_qk_lnorm: True
# TODO: switching to < 1 triggers triton-related issues.
# See https://github.com/ecmwf/WeatherGenerator/issues/1050
ae_global_att_dense_rate: 1.0
ae_global_block_factor: 64
ae_global_mlp_hidden_factor: 2
ae_global_trailing_layer_norm: False

ae_aggregation_num_blocks: 0
ae_aggregation_num_heads: 32
ae_aggregation_dropout_rate: 0.1
ae_aggregation_with_qk_lnorm: True
ae_aggregation_att_dense_rate: 1.0
ae_aggregation_block_factor: 64
ae_aggregation_mlp_hidden_factor: 2

decoder_type: PerceiverIOCoordConditioning #  Main options PerceiverIOCoordConditioning or Linear
pred_adapter_kv: False
pred_self_attention: True
pred_dyadic_dims: False
pred_mlp_adaln: True
num_class_tokens: 0
num_register_tokens: 0

# number of steps offset applied to first target window; if set to zero and forecast_steps=0 then
# one is training an auto-encoder
fe_num_blocks: 12
fe_num_heads: 16
fe_dropout_rate: 0.1
fe_with_qk_lnorm: True
# fe_diffusion_model builds the time-conditioned (DiT) backbone in ForecastingEngine and the
# forecast target/conditioning construction in model.py — shared by both engines.
# fe_flow_matching_model then wraps that backbone in FlowMatchingForecastEngine (checked first).
fe_diffusion_model: True
fe_flow_matching_model: True
fe_diffusion_model_conditioning: "forecast" # options: "date_time", "time", "forecast"
fe_diffusion_model_conditioning_type: "concatenate" # options: "cross_attn", "ada_ln", "additive", "concatenate"
fe_layer_norm_after_blocks: []  # Index starts at 0. Thus, [3] adds a LayerNorm after the fourth layer
fe_impute_latent_noise_std: 0.0  # 1e-4
# currently fixed to 1.0 (due to limitations with flex_attention and triton)
forecast_att_dense_rate: 1.0
with_step_conditioning: True # False

# --- Flow matching / score matching ---
fe_flow_path: condot              # Gaussian path: alpha_t = t, beta_t = 1 - t  (t=0 noise, t=1 data)
fe_flow_prediction_type: velocity # velocity (CFM) | noise (DSM/DDPM) | score
fe_flow_sampler: ode              # ode (deterministic flow, Euler+Heun) | sde (score-based, Euler-Maruyama)
fe_flow_num_ensemble_members: 1
fm_t_eps: 1.0e-3                  # clamp t to [t_eps, 1-t_eps]
fm_num_steps: 50                  # inference integration steps
fm_sde_sigma: 0.0                 # SDE diffusion coefficient (0 -> pure ODE)
fm_time_scale: 1000.0             # scale t before the sinusoidal time embedding (see NOTE below)
fm_c_in: 1.0                      # input preconditioning (identity for flow matching)
fm_diag_skip_steps: 1             # omit the first step (t~t_eps) from the sampling-diagnostics plot

# NOTE on fm_time_scale: the sinusoidal embedding uses a DDPM/DiT frequency ladder
# (max_period=10000) calibrated for integer timesteps in [0, 1000]. Feeding raw t in [0,1] would
# leave every sinusoid argument <= 1 rad, collapsing all channels into a near-linear regime
# (embeddings of t=0.2 and t=0.8 would be ~98% cosine-similar, i.e. the network could barely tell
# the noise end from the data end). x1000 maps t onto the range the ladder expects.

# t is drawn directly at the source (masking.py) as noise_level_rn ~ Unif[t_min, t_max];
# the engine/loss consume it as the path time t.
noise_distribution: uniform

frequency_embedding_dim: 256
embedding_dim: 512
# EDM-only knobs, unused by flow matching (kept for config/backbone compatibility).
sigma_min: 0.002
sigma_max: 80
sigma_data: 1.0
rho: 7
p_mean: 1.5
p_std: 1.2

healpix_level: 5

# Use 2D RoPE instead of traditional global positional encoding
rope_2D: True
mlp_type: swiglu
use_xsa: True

with_mixed_precision: True
with_flash_attention: True
compile_model: False
with_fsdp: False
attention_dtype: bf16
mixed_precision_dtype: bf16
mlp_norm_eps: 1e-5
norm_eps: 1e-4

latent_noise_kl_weight: 0.0 # 1e-5
latent_noise_gamma: 2.0
latent_noise_saturate_encodings: 5
latent_noise_use_additive_noise: False
latent_noise_deterministic_latents: True


#below for FE training only
freeze_modules: ".*latent_pre_norm.*|.*latent_heads.*|.*pred_heads.*|.*target_token_engines.*|.*embed_target_coords.*|.*encoder.*|.*StreamEmbedder_ERA5.*|.*embed_engine.*|.*embed_engine.*|.*ae_local_engine.*|.*ae_local_global_engine.*|.*ae_global_engine.*"
#below for DECODER training only
# freeze_modules: ".*latent_pre_norm.*|.*latent_heads.*|.*encoder.*|.*StreamEmbedder_ERA5.*|.*embed_engine.*|.*embed_engine.*|.*fe.*|.*ae_local_engine.*|.*ae_local_global_engine.*|.*ae_global_engine.*"
#below for FE and DECODER training
# freeze_modules: ".*latent_pre_norm.*|.*latent_heads.*|.*encoder.*|.*StreamEmbedder_ERA5.*|.*embed_engine.*|.*embed_engine.*|.*ae_local_engine.*|.*ae_local_global_engine.*|.*ae_global_engine.*"

load_chkpt: {'run_id': 'iy534ge3', 'epoch': -1}  # d2048 hl5 pre-training with z

norm_type: "LayerNorm"

#####################################

streams_directory: "./config/streams/era5_1deg_forecasting_d2048/"
streams: ???

# type of zarr_store
zarr_store: "zip" # "zarr" for LocalStore, "zip" for ZipStore

general:

  # mutable parameters
  istep: 0
  rank: ???
  world_size: ???

  multiprocessing_method: "fork"

  desc: ""
  run_id: ???
  run_history: []

# logging frequency in the training loop (in number of batches)
train_logging:
  terminal: 10
  metrics: 20
  checkpoint: 250
  log_grad_norms: False

# parameters for data loading
data_loading :

  num_workers: 12
  rng_seed: ???
  repeat_data_in_mini_epoch : False
  memory_pinning: True


# config for training
training_config:

  training_mode: ["masking","student_teacher"]

  num_mini_epochs: 128
  samples_per_mini_epoch: 4096
  shuffle: True

  start_date: 1979-01-01T00:00
  end_date: 2022-12-31T18:00

  time_window_step: 06:00:00
  time_window_len: 06:00:00

  learning_rate_scheduling :
    lr_start: 1e-6 #5e-5
    lr_max: 1e-5 #1e-4
    lr_final_decay: 1e-6
    lr_final: 0.0
    num_steps_warmup: 64
    num_steps_cooldown: 512
    policy_warmup: "cosine"
    policy_decay: "constant"
    policy_cooldown: "linear"
    parallel_scaling_policy: "sqrt"

  optimizer:
    grad_clip: 1.0
    weight_decay: 0.1
    log_grad_norms: False
    adamw :
      # parameters are scaled by number of DDP workers
      beta1 : 0.975
      beta2 : 0.9875
      eps : 2e-08

  losses : {
    "physical": {
        type: LossPhysical,
        weight: 0.0,
        loss_fcts: {
          "mse": {},
        },
        target_and_aux_calc: "Physical",
    },
    # NB keep the "latent_diff" key: it is the loss-term name the pipeline expects.
    # z_pre_norm now holds a velocity/noise/score, NOT a clean latent — so keep the physical
    # weight at 0.0 (decoding a velocity field into physical space is meaningless).
    "latent_diff": {
        type: LossFlowMatching,
        weight: 1.0,
        target_and_aux_calc: FlowMatchingTargetEncoder,
        loss_fcts: { "mse": { }, },
        }
  }

  model_input: {
    "forecasting" : {
      # masking strategy: "random", "healpix", "forecast"
      masking_strategy: "forecast",
      masking_strategy_config: {diffusion_rn: True, noise_distribution: uniform, t_min: 0.0, t_max: 1.0},
      num_steps_input: 2,
      num_samples: 1,
      }
    }

  target_input: {
    "forecasting" : {
      masking_strategy: "forecast",
      masking_strategy_config: {diffusion_rn: True, noise_distribution: uniform, t_min: 0.0, t_max: 1.0},
      num_steps_input: 1,
      num_samples: 1,
      }
    }

  forecast :
      time_step: 06:00:00
      num_steps: 1
      offset: 0
      policy: "fixed"


# validation config; full validation config is merge of training and validation config
validation_config:

  # Fixed path times t in (0,1) at which to evaluate during validation (each produces a separate
  # pass with independently logged metrics). NB these are *t values*, not eta — the flow-matching
  # analogue of the diffusion validation_noise_levels. They surface in the logs with the usual
  # suffix, formatted by Decimal.normalize(): 0.2 -> .eta2e-1, 0.4 -> .eta4e-1, etc.
  validation_noise_levels: [0.2, 0.4, 0.6, 0.8]

  samples_per_mini_epoch: 256
  shuffle: True

  start_date: 2023-10-01T00:00
  end_date: 2023-12-31T18:00

  # whether to track the exponential moving average of weights for validation
  validate_with_ema:
    enabled : True
    ema_ramp_up_ratio: 0.09
    ema_halflife_in_thousands: 1e-3

  # parameters for validation samples that are written to disk
  output : {
    num_samples: 0,
    normalized_samples: False,
    streams: null,
    }

  validate_before_training: False


# Tags for experiment tracking
wgtags:
  org: null
  issue: null
  exp: null
  grid: null
