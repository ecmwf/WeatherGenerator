# Plot config for JEPA pretraining runs (auto-generated)
train:
  plot:
    eplzv6bd:
      slurm_id: 0
      description: "JEPA default RMSNorm for attention"
    fsgs25zq-stage1:
      slurm_id: 0
      description: "JEPA default 2d rope for attention"
