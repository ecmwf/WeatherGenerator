version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

python:
  install:
    - requirements: packages/evaluate/docs/requirements.txt

sphinx:
  configuration: packages/evaluate/docs/conf.py